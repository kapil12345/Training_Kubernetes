apiVersion: v1
kind: Pod
metadata:
  name: sc-demo
spec:
  containers:
    - name: demo
      image: centos
      command: ["sleep", "30000"]
  securityContext:
    runAsUser: 10000
    runAsGroup: 1
