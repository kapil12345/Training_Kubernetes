apiVersion: v1
kind: Pod
metadata:
  name: httpd-x1
  labels:
    proj: x1
spec:
  containers:
  - name: myapp
    image: <Image>
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: <Port>
