apiVersion: v1
kind: Pod
metadata:
  name: httpd-readiness
  labels:
    name: httpd
spec:
  containers:
  - name: httpd
    image: httpd
    resources:
      limits:
        memory: "128Mi"
        cpu: "100m"
    ports:
      - containerPort: <Port>
